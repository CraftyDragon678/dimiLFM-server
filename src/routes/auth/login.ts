import { Router } from 'express';
import expressAsyncHandler from 'express-async-handler';

const router = Router();

router.post('/', expressAsyncHandler((req, res) => {

}))

export default router;
